# @voce-engine/contracts

Versioned public JSON contracts and schemas for VOCE. This package is data and types only.

> `0.1.0-rc.3` is a release candidate, not production-ready. Contracts may change before `0.1.0`.

```bash
npm install @voce-engine/contracts@0.1.0-rc.3
```
