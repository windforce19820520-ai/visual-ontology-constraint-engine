/** Standalone Host UI. Role/cardinality metadata comes from /api/meta; no ontology path or prompt template is embedded here. */
export const PLAYGROUND_HTML = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="description" content="Compile declared visual references through VOCE"><title>VOCE Playground</title>
<style>
:root{color-scheme:light;--ink:#18212f;--muted:#5b6878;--line:#d7dee8;--panel:#fff;--wash:#f4f7fb;--accent:#3b5ccc;--danger:#9a2d2d}*{box-sizing:border-box}body{margin:0;font:16px/1.5 system-ui,-apple-system,Segoe UI,sans-serif;color:var(--ink);background:var(--wash)}main{max-width:1180px;margin:0 auto;padding:24px 16px 64px}h1{margin:0 0 4px;font-size:clamp(1.6rem,4vw,2.4rem)}h2{margin:0 0 12px;font-size:1.18rem}.lede{margin:0;color:var(--muted);max-width:70ch}.tabs{display:flex;gap:8px;margin:16px 0}.tab{border:1px solid var(--line);border-radius:999px;padding:9px 16px;background:var(--panel);cursor:pointer}.tab[aria-selected=true]{background:var(--accent);border-color:var(--accent);color:#fff}.layout{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1.15fr);gap:16px;align-items:start}.panel{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:16px}.slot-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.slot{border:1px dashed #b8c3d2;border-radius:10px;padding:12px;background:#fbfcfe}.slot.required{border-style:solid}.slot label{display:block;font-weight:650;margin-bottom:6px}.hint{color:var(--muted);font-size:.86rem;margin:4px 0 8px}input[type=file],select,button{font:inherit}input[type=file]{width:100%}select{width:100%;border:1px solid var(--line);border-radius:8px;padding:9px;background:#fff}button.primary{border:0;border-radius:9px;padding:10px 15px;background:var(--accent);color:#fff;font-weight:700;cursor:pointer}button.primary:disabled{opacity:.55;cursor:not-allowed}.preview{display:none;width:100%;max-height:150px;object-fit:contain;margin-top:8px;border-radius:7px;background:#eef2f7}.preview.visible{display:block}.row{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:14px}.check{display:flex;gap:8px;align-items:flex-start;margin:14px 0}.notice{border-left:4px solid #d39d26;background:#fff8e8;padding:10px 12px;color:#634b13;font-size:.9rem}.status{margin:12px 0;min-height:1.5em;color:var(--muted)}.status.error{color:var(--danger)}.result{min-width:0}.result pre{white-space:pre-wrap;overflow:auto;max-height:420px;margin:0;background:#101827;color:#e6edf7;border-radius:9px;padding:12px;font:12px/1.45 ui-monospace,SFMono-Regular,Consolas,monospace}.result-section{margin-top:12px}.result-section summary{cursor:pointer;font-weight:700;padding:8px 0}.badge{display:inline-block;padding:3px 8px;border-radius:999px;background:#e8edf8;color:#2a438f;font-size:.8rem}@media(max-width:760px){main{padding:18px 12px 48px}.layout{grid-template-columns:1fr}.slot-grid,.row{grid-template-columns:1fr}.panel{padding:13px}.tabs{overflow:auto}}
</style></head><body><main>
<header><h1>VOCE Playground</h1><p class="lede">Declare what each reference is allowed to contribute, choose a composition preset, and inspect the compiled plan. This page compiles only; image rendering is disabled.</p></header>
<nav class="tabs" aria-label="Scenario"><button class="tab" type="button" data-scenario="virtual-tryon" aria-selected="true">Virtual Try-On</button><button class="tab" type="button" data-scenario="cosplay" aria-selected="false">Cosplay</button></nav>
<div class="layout"><section class="panel" aria-labelledby="input-title"><h2 id="input-title">References and composition</h2><div id="slots" class="slot-grid" aria-live="polite"></div><div id="supplemental"></div><div class="row"><div><label for="composition">Composition preset</label><select id="composition"><option value="">No preset</option></select></div><div><label for="provider">Provider profile</label><select id="provider"></select></div></div><p class="hint">Composition choices come from the public Core catalog. Provider profiles are allow-listed and transport remains off.</p><label class="check"><input id="rights" type="checkbox"><span>I have the right to use these images and understand that compile does not send them to a model.</span></label><div class="notice" role="note">Pose reference guidance: upload a ready-made skeleton image, action photo, or pose sketch. No pose extraction or editor is included.</div><button id="compile" class="primary" type="button" disabled>Compile plan</button><p id="status" class="status" role="status" aria-live="polite"></p></section>
<section class="panel result" aria-labelledby="result-title"><h2 id="result-title">Compiled result <span id="result-badge" class="badge">Waiting</span></h2><p class="hint">Observed facts and confirmed source bindings are empty until an approved interpreter supplies evidence.</p><div id="result-body"><p class="status">Choose references, confirm rights, and compile.</p></div></section></div></main>
<script>
(()=>{const state={scenario:'virtual-tryon',meta:null,controls:new Map(),sessionId:crypto.randomUUID?crypto.randomUUID():String(Date.now())},$=id=>document.getElementById(id),slots=$('slots'),supplemental=$('supplemental'),composition=$('composition'),provider=$('provider'),rights=$('rights'),compile=$('compile'),status=$('status'),resultBody=$('result-body'),badge=$('result-badge');const label=role=>role.replaceAll('-',' ').replace(/\b\w/g,c=>c.toUpperCase()),roles=()=>((state.meta.scenarios.find(item=>item.id===state.scenario)||{}).roles||[]);
function addControl(role,required,index){const key=role+':'+index,wrap=document.createElement('div');wrap.className='slot'+(required?' required':'');const id='file-'+key.replace(/[^a-z0-9]+/gi,'-'),title=document.createElement('label');title.htmlFor=id;title.textContent=label(role)+(required?' (required)':' (optional)');const hint=document.createElement('p');hint.className='hint';hint.textContent=role==='pose'?'Skeleton image, action photo, or pose sketch.':'The slot role is validated by the selected ScenarioPack.';const input=document.createElement('input');input.type='file';input.id=id;input.accept='image/png,image/jpeg,image/webp';input.setAttribute('aria-label',title.textContent);const preview=document.createElement('img');preview.className='preview';preview.alt='';input.addEventListener('change',()=>{const file=input.files&&input.files[0];if(!file)return;preview.src=URL.createObjectURL(file);preview.alt=label(role)+' local preview';preview.classList.add('visible')});wrap.append(title,hint,input,preview);slots.appendChild(wrap);state.controls.set(key,{role,input})}
function renderControls(){slots.replaceChildren();state.controls.clear();supplemental.replaceChildren();for(const role of roles())for(let i=0;i<role.minCount;i++)addControl(role.role,true,i);const optional=roles().filter(role=>role.maxCount>role.minCount);if(optional.length){const title=document.createElement('h3');title.textContent='Supplemental references';const select=document.createElement('select');select.setAttribute('aria-label','Supplemental reference role');for(const role of optional)select.appendChild(new Option(label(role.role),role.role));const add=document.createElement('button');add.type='button';add.className='primary';add.style.marginTop='8px';add.textContent='Add supplemental';const counts={};add.addEventListener('click',()=>{const role=select.value,count=counts[role]||0,policy=optional.find(item=>item.role===role);if(!policy||count>=policy.maxCount-policy.minCount)return;counts[role]=count+1;addControl(role,false,policy.minCount+count)});supplemental.append(title,select,add)}}
function renderMeta(){composition.replaceChildren(new Option('No preset',''));for(const item of state.meta.presets)composition.appendChild(new Option(item.id.replaceAll('-',' '),item.id));provider.replaceChildren();for(const item of state.meta.providers)provider.appendChild(new Option(item.label+(item.transportEnabled?'':' (transport off)'),item.id));renderControls()}
async function upload(file,role){const bytes=new Uint8Array(await file.arrayBuffer());let binary='';for(let i=0;i<bytes.length;i+=0x8000)binary+=String.fromCharCode(...bytes.subarray(i,i+0x8000));const response=await fetch('/api/upload',{method:'POST',headers:{'content-type':'application/json','x-playground-session':state.sessionId},body:JSON.stringify({fileName:file.name,mediaType:file.type,bytesBase64:btoa(binary),role,poseSourceKind:role==='pose'?'pose-sketch':undefined})});if(!response.ok)throw new Error((await response.json()).error||'Upload failed');return response.json()}
function showResult(value){badge.textContent='Compiled';resultBody.replaceChildren();for(const pair of [['Human Plan',value.humanPlan],['Constraints',value.constraintIR],['Prompt IR',value.promptIR],['Trace',value.trace],['Provider request',value.providerRenderRequest]]){const details=document.createElement('details');details.className='result-section';details.open=pair[0]==='Human Plan';const summary=document.createElement('summary');summary.textContent=pair[0];const pre=document.createElement('pre');pre.textContent=JSON.stringify(pair[1],null,2);details.append(summary,pre);resultBody.append(details)}}
async function runCompile(){status.className='status';status.textContent='Uploading references and compiling…';compile.disabled=true;try{const assets=[],declaredRoles=[];for(const control of state.controls.values()){const file=control.input.files&&control.input.files[0];if(!file)continue;const uploaded=await upload(file,control.role);assets.push(uploaded.artifact);declaredRoles.push({assetId:uploaded.artifact.id,role:control.role,order:0})}const response=await fetch('/api/compile',{method:'POST',headers:{'content-type':'application/json','x-playground-session':state.sessionId},body:JSON.stringify({scenarioId:state.scenario,assets,declaredRoles,compositionSelections:composition.value?[{presetId:composition.value}]:[],providerProfileId:provider.value,rightsConfirmed:rights.checked})});const value=await response.json();if(!response.ok)throw new Error(value.error||'Compilation failed');showResult(value);status.textContent='Plan compiled offline. Render remains disabled.'}catch(error){badge.textContent='Blocked';resultBody.replaceChildren();status.className='status error';status.textContent=error instanceof Error?error.message:'Compilation failed'}finally{compile.disabled=!rights.checked}}
document.querySelectorAll('[data-scenario]').forEach(button=>button.addEventListener('click',()=>{state.scenario=button.dataset.scenario;document.querySelectorAll('[data-scenario]').forEach(item=>item.setAttribute('aria-selected',item===button?'true':'false'));renderControls()}));rights.addEventListener('change',()=>{compile.disabled=!rights.checked});compile.addEventListener('click',runCompile);fetch('/api/meta').then(response=>response.json()).then(meta=>{state.meta=meta;renderMeta()}).catch(()=>{status.className='status error';status.textContent='Could not load Playground metadata.'})})();
</script><script>const byokLabel=document.createElement('label');byokLabel.htmlFor='playground-byok-key';byokLabel.textContent='Optional BYOK API key (request-scoped; transport off)';const byok=document.createElement('input');byok.id='playground-byok-key';byok.type='password';byok.autocomplete='off';byok.spellcheck=false;byok.placeholder='Never stored';byok.style.cssText='display:block;width:100%;max-width:520px;padding:9px;border:1px solid #d7dee8;border-radius:8px;margin:8px 0 16px';document.querySelector('header').after(byokLabel,byok);</script></body></html>`
