# Optional operator-managed Cloudflare Workers AI credential overlay.
# Install as /etc/voce-playground/cloudflare.env, owned by root:root with mode 0600.
# Never commit, log, export, or return real values.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
