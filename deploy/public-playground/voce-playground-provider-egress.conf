[Service]
# Required only when one or more real Provider transports are explicitly enabled.
# Empty assignments clear the compile-only unit's IP address allow/deny lists.
# Provider destinations remain fixed and validated by the application adapters.
IPAddressDeny=
IPAddressAllow=
