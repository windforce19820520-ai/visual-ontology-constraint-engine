[Unit]
Description=VOCE Public Playground
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=voce
Group=voce
WorkingDirectory=/opt/voce-playground/current
Environment=NODE_ENV=production
EnvironmentFile=/etc/voce-playground/voce-playground.env
# Optional operator-managed Cloudflare credential file. Keep it root:root 0600.
EnvironmentFile=-/etc/voce-playground/cloudflare.env
ExecStart=/usr/bin/node playground/dist/server-entry.js
Restart=on-failure
RestartSec=5s
KillSignal=SIGTERM
TimeoutStopSec=30s
UMask=0077

# The Host keeps request-scoped images in memory and needs no writable app path.
NoNewPrivileges=true
PrivateTmp=true
PrivateDevices=true
ProtectSystem=strict
ProtectHome=true
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectKernelLogs=true
ProtectControlGroups=true
ProtectClock=true
ProtectHostname=true
RestrictSUIDSGID=true
LockPersonality=true
RestrictRealtime=true
SystemCallArchitectures=native
CapabilityBoundingSet=
AmbientCapabilities=
RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6
# Compile-only defense in depth: accept Nginx on loopback, deny external calls.
# Install the reviewed provider-egress drop-in only when a real transport is
# explicitly authorized; the application itself allow-lists Provider endpoints.
IPAddressDeny=any
IPAddressAllow=localhost
MemoryMax=1536M
TasksMax=128
LimitNOFILE=4096

[Install]
WantedBy=multi-user.target
